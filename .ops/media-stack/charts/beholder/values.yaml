# Default values for beholder.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: jaredallard/media-stack
  tag: latest
  pullPolicy: Always

environment: 'debug'

resources: {}

nodeSelector: {}

tolerations: []

affinity: {}
